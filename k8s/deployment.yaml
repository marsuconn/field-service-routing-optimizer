# ============================================================
# deployment.yaml — Kubernetes Deployment for Field Service Router
# ============================================================
# This runs the FastAPI re-optimization service (Level 2)
# as a long-running Kubernetes pod.
#
# The Airflow DAG (Level 1) calls this service's /optimize
# endpoint, and mid-day events call /reoptimize.
#
# Apply:  kubectl apply -f k8s/deployment.yaml
# Check:  kubectl get pods -n field-service
# Logs:   kubectl logs -n field-service deployment/field-router
# ============================================================

apiVersion: apps/v1
kind: Deployment
metadata:
  name: field-router
  namespace: field-service
  labels:
    app: field-router
    team: routing-optimization
    managed-by: argocd          # Tells Argo CD this is its resource
spec:
  replicas: 2                   # 2 pods for high availability
  selector:
    matchLabels:
      app: field-router
  template:
    metadata:
      labels:
        app: field-router
    spec:
      containers:
        - name: router
          image: field-router:0.1.0
          ports:
            - containerPort: 8000
              name: http

          # Resource requests/limits
          # OR-Tools needs decent CPU for solving
          resources:
            requests:
              cpu: "500m"       # 0.5 CPU cores minimum
              memory: "512Mi"
            limits:
              cpu: "2000m"      # burst up to 2 cores during solve
              memory: "2Gi"

          # Environment variables
          # In production: Snowflake creds, API keys, etc.
          env:
            - name: ENVIRONMENT
              value: "production"
            - name: LOG_LEVEL
              value: "INFO"
            - name: SOLVE_TIME_LIMIT
              value: "30"
            # Snowflake credentials from Kubernetes Secret
            - name: SNOWFLAKE_ACCOUNT
              valueFrom:
                secretKeyRef:
                  name: field-service-secrets
                  key: snowflake-account
            - name: SNOWFLAKE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: field-service-secrets
                  key: snowflake-password

          # Health checks — Kubernetes uses these to manage the pod
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10

---

# ============================================================
# service.yaml — Kubernetes Service (internal load balancer)
# ============================================================
# This gives the routing API a stable internal DNS name:
#   http://field-router-svc.field-service:8000
#
# The Airflow DAG and event triggers call this address.
# ============================================================

apiVersion: v1
kind: Service
metadata:
  name: field-router-svc
  namespace: field-service
  labels:
    app: field-router
spec:
  selector:
    app: field-router
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  type: ClusterIP              # Internal only (not exposed to internet)

---

# ============================================================
# namespace.yaml — Kubernetes Namespace
# ============================================================

apiVersion: v1
kind: Namespace
metadata:
  name: field-service
  labels:
    team: routing-optimization
